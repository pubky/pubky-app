import { ProfileUnlocked } from '@/organisms/ProfileUnlocked/ProfileUnlocked';

/**
 * ProfileUnlockedPage Template
 *
 * Template for displaying the content the current user has unlocked.
 * Delegates all logic to the ProfileUnlocked organism.
 */
export function ProfileUnlockedPage() {
  return <ProfileUnlocked />;
}
