export const TRUNCATION_LIMIT = 300;
export const POST_TEXT_PREVIEW_MAX_LINES = 6;

/** Touch duration required before a compact post URL reveals its full value. */
export const POST_LINK_LONG_PRESS_DELAY_MS = 400;
