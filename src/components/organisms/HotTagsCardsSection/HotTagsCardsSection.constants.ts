// Avatar limits based on screen size to prevent overflow
export const MAX_AVATARS_MOBILE = 2; // < 640px (cards stacked, full width)
export const MAX_AVATARS_DEFAULT = 4; // 640px - 1280px (cards in row)
export const MAX_AVATARS_XL = 6; // ≥ 1280px (cards in row, wider screen)
