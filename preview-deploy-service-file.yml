apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: SHOULD_BE_CHANGED
  labels:
    cloud.googleapis.com/location: europe-west6
    run.googleapis.com/satisfiesPzs: 'true'
    managed-by: github-actions
  annotations:
    run.googleapis.com/invoker-iam-disabled: 'true'
    run.googleapis.com/iap-enabled: 'true'

spec:
  template:
    metadata:
      labels:
        run.googleapis.com/startupProbeType: Default
      annotations:
        autoscaling.knative.dev/maxScale: '2'
        run.googleapis.com/container-dependencies: '{"nginx":["pubky-app-preview-1"]}'
        run.googleapis.com/cpu-throttling: 'true'
        run.googleapis.com/startup-cpu-boost: 'true'
        run.googleapis.com/sessionAffinity: 'true'
    spec:
      containerConcurrency: 80
      timeoutSeconds: 300
      containers:
        - name: nginx
          image: >-
            europe-west6-docker.pkg.dev/infra-464608/synonym-private-repo/nginx-normilizer:latest
          ports:
            - name: http1
              containerPort: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
          startupProbe:
            timeoutSeconds: 240
            periodSeconds: 240
            failureThreshold: 1
            tcpSocket:
              port: 8080
        - name: pubky-app-preview-1
          image: SHOULD_BE_UPDATED
          env:
            - name: PUBKY_RUNTIME_TESTNET
              value: "false"
            - name: PUBKY_RUNTIME_HOMESERVER
              value: "ufibwbmed6jeq9k4p583go95wofakh9fwpp4k734trq79pd9u1uy"
            - name: PUBKY_RUNTIME_NEXUS_URL
              value: "https://nexus.staging.pubky.app"
            - name: PUBKY_RUNTIME_CDN_URL
              value: "https://nexus.staging.pubky.app/static"
            - name: PUBKY_RUNTIME_HOMEGATE_URL
              value: "https://homegate.staging.pubky.app"
            - name: PUBKY_RUNTIME_PKARR_RELAYS
              value: '["https://pkarr.pubky.app","https://pkarr.pubky.org"]'
            - name: PUBKY_RUNTIME_DEFAULT_HTTP_RELAY
              value: "https://httprelay.staging.pubky.app/inbox"
          resources:
            limits:
              cpu: '1'
              memory: 512Mi
          startupProbe:
            timeoutSeconds: 240
            periodSeconds: 240
            failureThreshold: 1
            tcpSocket:
              port: 3000
